# VoiceClaw Frontend - Cloudflare Pages Configuration
name = "voiceclaw"
compatibility_date = "2024-01-01"

# Build configuration
[build]
command = "npm run build"
output = "dist"

# Environment variables (set in Cloudflare dashboard for production)
# VITE_WS_URL = "ws://localhost:8000/ws"  # Set this to your backend URL
